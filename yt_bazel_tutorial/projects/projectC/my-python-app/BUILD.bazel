# WORKSPACE 的 python_deps
# load("@python_deps://:requirements.bzl", "requirements")

py_binary(
    name = "app",
    srcs = [
        "main.py"
    ],
    deps = [
        "//projects/projectB/calculator:calc",
    ],
    main = "main.py",
)